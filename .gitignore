# --- Secrets & runtime state (NEVER commit) ---
.env

# --- n8n / Postgres runtime data ---
data/
postgres/
backups/

# --- Database dumps ---
*.sql
*.sql.gz

# --- Logs ---
*.log

# --- Editor / OS cruft ---
.DS_Store
.idea/
*.swp
*.swo

# VS Code: ignore most workspace noise, but allow committing a couple of
# workspace-level convenience files if you want (safe for public repos).
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json

# --- Temporary / scratch files ---
*.tmp
*.bak